openapi: 3.2.0
info:
  version: v1
  title: Legacy API (v1) V1 Expenses API
  description: ''
servers:
- url: https://app.practicepanther.com
tags:
- name: v1 - Expenses
paths:
  /api/internal/v1/tenants/{tenantId}/expenses:
    post:
      tags:
      - v1 - Expenses
      operationId: V1Expenses_Create
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
            text/json:
              schema:
                type: object
      security:
      - oauth2:
        - full
      summary: V1 expenses create
      x-summary-source: derived
components:
  securitySchemes:
    oauth2:
      type: oauth2
      description: OAuth 2.0 - Authorization Code Grant
      flows:
        authorizationCode:
          authorizationUrl: /OAuth/Authorize
          tokenUrl: /OAuth/Token
          scopes:
            full: Read/Write access to all resources
